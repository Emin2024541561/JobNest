<?xml version="1.0" encoding="utf-8" ?>
<ContentPage x:Class="JobNest.Views.NotificationsPage"
             xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             Title="Obavijesti"
             BackgroundColor="White">

    <StackLayout Padding="20" Spacing="15">
        <Label Text="Obavijesti"
               FontSize="24"
               FontAttributes="Bold"
               TextColor="#2E7D32"/>

        <CollectionView x:Name="NotificationsCollectionView">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Frame BackgroundColor="#E8F5E9"
                           CornerRadius="10"
                           Padding="10"
                           Margin="0,5">
                        <StackLayout>
                            <Label Text="{Binding}"
                                   FontSize="16"
                                   TextColor="Black"/>
                        </StackLayout>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </StackLayout>
</ContentPage>
